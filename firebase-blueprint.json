{
  "entities": {
    "Analysis": {
      "title": "Analysis",
      "description": "A saved prompt analysis for a user",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user who created this analysis"
        },
        "originalPrompt": {
          "type": "string",
          "description": "The original prompt submitted by the user"
        },
        "useCase": {
          "type": "string",
          "description": "The selected use case for the prompt"
        },
        "analysis": {
          "type": "string",
          "description": "JSON stringified PromptAnalysis object"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the analysis was saved"
        }
      },
      "required": ["userId", "originalPrompt", "useCase", "analysis", "createdAt"]
    },
    "Feedback": {
      "title": "Feedback",
      "description": "User feedback on the app or prompt analysis",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user who submitted feedback (optional)"
        },
        "text": {
          "type": "string",
          "description": "The feedback content"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the feedback was submitted"
        }
      },
      "required": ["text", "createdAt"]
    }
  },
  "firestore": {
    "/analyses/{analysisId}": {
      "schema": {
        "$ref": "#/entities/Analysis"
      },
      "description": "Stores user prompt analyses"
    },
    "/feedback/{feedbackId}": {
      "schema": {
        "$ref": "#/entities/Feedback"
      },
      "description": "Stores user feedback"
    }
  }
}
